import { describe, test } from 'vitest';

describe('InlineFormatter - code_inline & breaks', () => {
  
  test.todo('converts softbreak to newline');

  test.todo('converts hardbreak <br> to newline');

});

