import { describe, test } from 'vitest';


describe('DocxConverter - fenced code blocks', () => {
  test.todo('turns a ```js code fence into a single monospace paragraph');
});

describe('DocxConverter - paragraphs', () => {
  test.todo('renders a single paragraph correctly');
});

describe('DocxConverter - lists', () => {
  test.todo('renders a simple bullet list');

  test.todo('renders a simple ordered list');
});

describe('DocxConverter - hr & blockquote', () => {
  test.todo('renders a horizontal rule');

  test.todo('renders a blockquote');
});
